# Rust
/target
**/target
**/*.rs.bk

# Tau project-local state
/.tau

# OS / editor junk
.DS_Store
*.swp
*.swo
.idea/
.vscode/
.claude/
.claude/worktrees/

# Coverage
*.profraw
tarpaulin-report.html
target/agent-*
target/term

# QEMU core dumps (from Podman x86_64 emulation attempts)
qemu_*.core

# cargo-mutants output (mutation testing — see docs/how-to/run-mutation-testing.md)
mutants.out/
mutants.out.old/

# Out-of-workspace test fixtures generate their own lockfiles on first build.
crates/tau-mcp-tokio/tests/fixtures/mock-mcp-server/Cargo.lock
crates/tau-wasm-host/tests/fixtures/fs-probe/Cargo.lock
crates/tau-wasm-host/tests/fixtures/http-probe/Cargo.lock

# Python bytecode cache (scripts/verify-sync-config.py and other helper scripts).
__pycache__/

# Superpowers brainstorming visual companion (local only)
.superpowers/
