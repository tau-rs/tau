VISION FIXTURE — expected transcript (target state).
Scene: agents/triage.md gained a tool, but tau.gen.ts was not regenerated.
Drift between the vocabulary and the typed bridge is LOUD, never silent.

$ npx tau check
error[E-GEN-STALE]: tau.gen.ts is stale
  registry hash sha256:9d44be02c1a7 ≠ stamped sha256:4f2c81d09aa3
  = the vocabulary changed after generation
  help: run `tau dev` (regenerates and re-stamps), then commit tau.gen.ts

$ echo $?
1
