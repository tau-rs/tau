VISION FIXTURE — expected transcript (target state).
Scene: tau/agents/triage.md gained a tool, but tau/gen was not
regenerated (someone bypassed tau dev / postinstall). Gen is gitignored
and reproducible, so the guarantee is FRESHNESS, verified everywhere it
matters: drift between the vocabulary and the typed client is loud,
never silent.

$ npx tau check
error[E-GEN-STALE]: tau/gen is stale
  registry hash sha256:9d44be02c1a7 ≠ generated-from sha256:4f2c81d09aa3
  = the vocabulary changed after generation
  help: run `tau gen` (or `tau dev`, which watches), then re-run check

$ echo $?
1
