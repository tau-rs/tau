[package]
name = "embed-native-workflow-lib"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
tau-runtime-core = { path = "../../../crates/tau-runtime-core", default-features = false, features = ["wasm-interpreter"] }

[lints]
workspace = true
