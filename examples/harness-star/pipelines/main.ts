// VISION FIXTURE — target state. The synth entry: posture C uses classic
// flow-lane authoring; the harness is a *declaration* over ordinary
// pipelines, not a different way of writing them (invariant 12).

import "./oncall-investigate";
import "./runbook-exec";
