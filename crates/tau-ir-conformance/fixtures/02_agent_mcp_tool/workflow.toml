packages = ["mock-llm"]

[project]
name = "fixture-02"

[models.mock-1]
backend = "mock-llm"
model = "mock-1"

[agents.weatherman]
display_name = "Weather Man"
package      = "weatherman@^0.1"
model        = "mock-1"
tool_refs    = ["weather"]
max_turns    = 2

[tools.weather]
mcp         = "https://mcp.weather.example.com"
description = "Look up current weather."
capabilities = [{ kind = "net.http" }]
