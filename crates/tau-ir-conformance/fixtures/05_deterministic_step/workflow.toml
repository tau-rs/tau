packages = ["mock-llm"]

[project]
name = "fixture-05"

[models.mock-1]
backend = "mock-llm"
model = "mock-1"

[agents.solo]
display_name = "Solo"
package      = "p@^0.1"
model        = "mock-1"
tool_refs    = ["normalize"]
max_turns    = 2

[steps.normalize]
deterministic = "parse_celsius"
input_schema  = {}
output_schema = {}
