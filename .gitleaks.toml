# gitleaks configuration for the tau repository.
# See https://github.com/gitleaks/gitleaks#configuration

title = "tau gitleaks config"

[extend]
# Extend the default ruleset.
useDefault = true

[[allowlists]]
description = "False positive: cargo cache key strings matched by generic-api-key rule"
# `cargo-fuzz-bin-v1` in fuzz-nightly.yml is a GitHub Actions cache key, not a secret.
regexes = ['''cargo-fuzz-bin-v\d+''']
paths = ['''.github/workflows/fuzz-nightly\.yml''']

[[allowlists]]
description = "False positive: generic-api-key rule matches the phrase 'parent/child' in trace-TUI design docs"
# The generic-api-key rule keys off the nearby word "token" ("...token counts,
# parent/child nesting...") and flags the literal phrase `parent/child` as a
# secret. These are authored design/plan prose (no secrets), and because the
# gitleaks CI job scans all branch history via `--log-opts "--all"`, rewording
# the current file cannot clear the historical commits — so this must be
# allowlisted by path.
regexes = ['''^parent/child$''']
paths = [
    '''docs/superpowers/specs/2026-08-21-execution-trace-tui-design\.md''',
    '''docs/superpowers/plans/2026-08-21-execution-trace-tui-m1\.md''',
]
